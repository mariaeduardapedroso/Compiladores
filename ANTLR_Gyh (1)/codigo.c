#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <string.h>


int main(void){

	int fatorial;

	int parametro;

	double fato;

	scanf("%d", &parametro);
	printf("%d",fatorial);
	parametro = 10;
	 if( fatorial>fato E parametro<=0 ){

	fatorial = fato+parametro;
	fato = fato+parametro;
	parametro = 0;
	 }
	 else {

	fato = fato+parametro;	 }

	return 0;
}
